[project]
name = "merken"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dishka>=1.9.1",
    "httpx>=0.28.1",
    "msgspec>=0.20.0",
    "pre-commit>=4.5.1",
]

[dependency-groups]
dev = [
    "ty>=0.0.26",
]
